export interface SponsorTokens {
  sponsor_name: string;
  sponsor_photo: string;
  /** Full wa.me click-to-chat URL */
  sponsor_whatsapp: string;
  /** The only referral link that exists — CaryPact issues it, BOT Chain doesn't. */
  carypact_url: string;
  funnel_url: string;
}
